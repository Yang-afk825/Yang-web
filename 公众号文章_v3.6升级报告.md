# Yang-Web v3.6 发布：从"一枪一个"到"一条链打穿"——CTF 工具箱学会了走多步棋

> 上一版我在文末立了个 flag：要做多阶段攻击链。这一版，把它交付了。

---

## 起因：一道"打不穿"的题

v3.0 那篇文章里，我炫耀过一键秒 `system($_POST['a'])` 的爽感——0.3 秒从分析到拿 Flag。

但没过几天就翻车了。

那道题长这样：源码里没有 `system()`，只有一个 `include($_GET['file'])`。这是标准的 LFI，可 flag 不在能直接读的地方，得走三步：

```
1. include 读日志文件 /var/log/nginx/access.log
2. 用 User-Agent 往日志里写一句 <?php system($_GET['c']); ?>
3. 再 include 这个被"投毒"的日志 → RCE → 读 flag
```

v3.0 的引擎当场懵了。它只会**发单发子弹**：找到一个注入点，把一堆 payload 并发轰过去，指望其中一发命中。可这道题的 Flag 藏在"第三步"，前两步不铺垫，第三步永远打不响。

**单点爆破打不穿需要走好几步的题。** 这就是 v3.6 要解决的核心问题。

---

## 核心变化：从"单点 Payload"到"多阶段攻击链"

### 主角登场：multi_stage.py（多阶段攻击引擎）

v3.0 的模型是"一次性":

```
分析 → 并发轰炸 → 拿到 Flag / 失败
```

v3.6 的模型是"走一步看一步":

```
阶段检测 → 攻击 → 响应分析 → 跳转追踪 → 下一阶段 → … → Flag
   ↑______________________________________________|
              (拿到中间产物就进入下一环)
```

它把一次攻击拆成"阶段"，每打完一步就**读懂这一步的响应**——拿到了什么文件路径？回显里有没有新的注入点？跳转到哪去了？——然后据此决定下一步怎么打，像串珠子一样把攻击链自动拼起来，直到读出 Flag。

回到上面那道 LFI 题：v3.6 会自己走完"读日志 → 投毒 → 再包含"三步，一路推到 flag。

---

## 一次上齐：11 个新解题引擎

光有一个总框架不够，得有"能打各种题型"的专项弹药。这一版一口气补了 11 个引擎：

### 高级扫描引擎 · advanced_scanner.py

把随波逐流式的目录扫描做成了带脑子的版本：

- **字典目录/文件爆破**——多层字典，挖备份、源码泄露、后台
- **Response Diffing 精准检测**——不靠状态码猜，靠响应体差异判断"真命中还是假 200"
- **内置攻击链**——`LFI→日志投毒→RCE`、`LFI→PHP Filter Chain`、`SSTI→Jinja2 RCE` 都做成了开箱即用的链
- **HTTP 方法自适应**——GET 打不动自动换 POST

### PHP 三兄弟

CTF Web 里 PHP 题量最大，这次专门补齐了三个高频题型：

| 引擎 | 打什么 |
|------|--------|
| `php_unserialize.py` | 反序列化：`__wakeup` 绕过（CVE-2016-7124）、`==` 弱类型绕过、private/protected 属性 `\0` 编码 |
| `php_lfi.py` | 文件包含：`file://`/`php://filter`/`php://input`/`data://` 协议约束自动识别 + flag 文件自动发现 |
| `php_eval_rce.py` | MD5 碰撞（数组绕过 `md5($a)===md5($b)`）+ `eval()` 执行 + WAF 关键字黑名单绕过 |

### bashFuck 无字母 RCE · bashfuck.py + bashfuck_solver.py

题目把字母数字全过滤了，只剩符号，怎么执行命令？

用 `$((2#二进制))` 把二进制拼成任意数字，再用 ANSI-C 的 `$'\ooo'` 把八进制转成 ASCII 字符，纯符号构造出完整命令。引擎覆盖 `system/exec/passthru`，自动过 WAF，GET/POST 参数自动探测。

### SSRF DNS Rebinding→RCE · ssrf_rebind.py

针对 Flask/Django 用 `socket.gethostbyname` 做 SSRF 黑名单校验的场景：检测 DNS 解析逻辑，用 DNS 重绑定 + 十进制/八进制 IP + `nip.io`/`sslip.io` 绕过黑名单，打到内网。

### SQLi-LABS 专项求解器 · sqli_labs_solver.py

经典靶场 Less-1 ~ Less-75 全映射：自动探测闭合方式、列数、注入类型、数据库指纹，支持**单关求解**、**一键批量通关**，还有**交互模式**手写 SQL 逐关练习。

### JS / 客户端挑战求解器 · js_challenge_solver.py

处理浏览器端的 CTF：游戏类（2048/贪吃蛇/FlappyBird）自动提取胜利条件和作弊码、JS 加解密逆向、直接在 JS/HTML 里搜硬编码 Flag。

### 智能一键解题总调度 · smart_solver.py

上面这么多引擎，谁来指挥？`smart_solver` 做总调度：先用**问题分类器**判断这题是 Web / PWN / Reverse / Crypto / Misc / Blockchain，再按分类**策略路由**到合适的引擎，多引擎自动编排。你只管把题目丢进来。

### 附赠：CTF+ 批量求解器 · ctfplus_batch_solver.py

读取 CTF+ 平台的题目 JSON，自动分类并批量开打，适合刷整套靶场。

---

## 顺手：脚本库 41 → 51

内嵌 CTF 脚本库从 41 个扩到 51 个，`registry` 顺便做了结构化重构——引入 `ScriptMeta` 类型定义，脚本的名称/分类/依赖/入口全部规范化，GUI 里检索和依赖检查更稳。

---

## 填坑：删掉了一个模块

这一版清理时删了 `payloads/rce.py`。有人可能会担心 GUI 的 RCE 面板炸了——放心，RCE 数据已经统一由 `php` 模块提供，GUI 那处 import 本身带 fallback，删掉之后跑了一遍导入测试，`import yang_web.gui` 照常通过。**留着的死代码比缺的功能更危险，该删就删。**

---

## 完整改动清单

| 模块 | 变化 |
|------|------|
| `core/multi_stage.py` | 🆕 多阶段攻击引擎（约 900 行） |
| `core/advanced_scanner.py` | 🆕 高级扫描 + 攻击链引擎（约 1100 行） |
| `core/smart_solver.py` | 🆕 智能一键解题总调度 |
| `core/php_unserialize.py` | 🆕 PHP 反序列化求解 |
| `core/php_lfi.py` | 🆕 PHP 文件包含求解 |
| `core/php_eval_rce.py` | 🆕 PHP MD5 碰撞 + eval RCE |
| `core/bashfuck*.py` | 🆕 bashFuck 无字母 RCE |
| `core/ssrf_rebind.py` | 🆕 SSRF DNS Rebinding |
| `core/sqli_labs_solver.py` | 🆕 SQLi-LABS Less-1~75 |
| `core/js_challenge_solver.py` | 🆕 JS/客户端挑战 |
| `scripts/registry.py` | 结构化重构，脚本 41 → 51 |
| `url_analyzer / gui / ssti / jwt` | 新引擎接入智能攻击面板 |

一次提交：**24 个文件，+11034 / −976 行。** 依然**零第三方依赖**，全部基于 Python 标准库。

---

## 使用方式

```bash
cd Yang-web
python -m yang_web.gui
```

1. 粘贴目标 URL
2. 点 **分析** → 源码指纹 + 漏洞识别
3. 点 **🚀 一键解题** → 智能分类 → 多阶段攻击链 → 拿 Flag

---

## 下一步

- **攻击链可视化**——把 multi_stage 走过的每一步画成流程图
- **WAF 指纹库扩充**——自动识别更多 WAF 并匹配绕过策略
- **PWN / Crypto 自动化**——smart_solver 目前 Web 最强，其余方向继续补齐
- **HTML 解题报告导出**——一键生成 WriteUp 草稿

---

## 结语

v3.0 学会了"读懂一步"，v3.6 学会了"走完一条链"。

CTF 里真正难的题，几乎没有一枪毙命的——都是 LFI 套日志投毒、反序列化套链、SSRF 套内网。工具能不能打穿，看的不是 payload 多不多，而是**会不会走多步棋**。

这一版，Yang-Web 学会了走棋。

---

*Yang-Web v3.6 | 开源 CTF 工具箱 | 零依赖 | Python 3.8+*

*GitHub：https://github.com/Yang-afk825/Yang-web · 觉得有用点个 ⭐*
