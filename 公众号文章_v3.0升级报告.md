# Yang-Web v3.0 发布：从"猜"到"懂"——让CTF工具箱真正读懂漏洞

> 一键粘贴URL，0.3秒拿Flag。这次不是魔法，是源码指纹。

---

## 起因：一个让人尴尬的测试结果

前两周，我用 Yang-Web 测试了一道简单的 CTF RCE 题目。

题目代码长这样：

```php
<?php
highlight_file(__FILE__);
system($_POST['a']);
```

明晃晃的 `system($_POST['a'])`，任何有点经验的 CTF 选手看一眼就知道：**POST 传参 `a` → 命令执行**。

但 Yang-Web v2.0 给出的分析结果是：

| 漏洞类型 | 置信度 |
|---------|--------|
| SQL 注入 | 30% |
| XSS     | 25% |

**完全瞎猜。** RCE 一个字没提。

Flag 自然也没拿到。

于是我花了几天时间，把整个智能攻击引擎推倒重来。这篇文章就是升级报告。

---

## 核心思路：从"特征匹配"到"源码理解"

v2.0 的分析逻辑本质上是**猜**：看 URL 参数名像不像 `id`、路径有没有 `.php`，然后拍脑袋给个概率。

v3.0 的核心变化只有一句话：**先读源码，再下判断。**

### 新增：SmartFingerprinter（智能指纹识别器）

```python
class SmartFingerprinter:
    """Step 0：在任何攻击之前，先读懂响应里写了什么"""
    
    PHP_DANGER_FUNCTIONS = {
        'system(':     ('RCE', 95),
        'exec(':       ('RCE', 95),
        'shell_exec(': ('RCE', 95),
        'eval(':       ('RCE', 90),
        'include($_':  ('LFI', 85),
        'mysqli_query': ('SQLi', 80),
    }
```

对目标发起一次无害的 GET 请求，拿到响应后立刻做三件事：

1. **PHP 危险函数检测** —— 扫 `system(`、`eval(`、`shell_exec(` 等关键词
2. **参数提取** —— 识别 `$_GET['id']`、`$_POST['cmd']` 等变量
3. **CMS/WAF 指纹** —— WordPress、ThinkPHP、Cloudflare、阿里云 WAF

知道目标用了什么技术栈，才知道该打什么 payload。

---

## 三大引擎，各司其职

### 1. ConcurrentEngine（并发攻击引擎）

```
旧: 串行发包 → 20个payload = 40秒+
新: 15线程并发 → 29个payload = 0.1秒
```

- ThreadPoolExecutor 并发执行
- 指数退避重试（最多 3 次）
- 发现 Flag 立即停止所有任务
- 线程安全：Lock 保护共享状态

### 2. AdaptiveScheduler（自适应调度器）

不再"一把梭"打所有 payload，而是根据指纹结果智能排序：

| 识别特征 | 优先级调整 |
|---------|-----------|
| 源码含 `system()` | RCE payload 排第一 |
| 源码含 `include($var)` | LFI 排第一 |
| 检测到 `$_POST` | 自动切换 POST 方法 |
| ThinkPHP 指纹 | Spring 类 payload 降权 |

低信心的猜测（< 20%）直接跳过，不打无效载荷。

### 3. 增强版 analyze_url

原有参数匹配 + 新 Step 0 源码指纹 + 交叉验证：

```
URL → 参数提取 + 路径匹配 + 页面爬取
     ↓
  源码指纹分析（NEW）
     ↓
  交叉加权 → 置信度重算
     ↓
  漏洞卡片 + 指纹数据
```

---

## 实战验证：同一道题，天壤之别

**测试目标：** HelloCTF RCE-labs
```
http://80-dd97d346-4448-4740-84d8-7c00b6a0c1f1.challenge.ctfplus.cn
```

### v2.0 表现

```
分析结果：
  SQLi  30%  ← 错的
  XSS   25%  ← 错的

一键解题：不适用（连漏洞类型都搞错了）
Flag：❌ 未获取
```

### v3.0 表现

```
指纹分析：
  [95%] system() 命令执行  ← 直接从源码读到
  [95%] exec() 命令执行
  [95%] shell_exec() 命令执行
  $POST["a"]               ← 自动识别 POST 参数

分析结果：
  RCE   100%  params=['a']  ← 精准
  PHP    60%
  SQLi   30%
  XSS    25%

并发攻击：29 任务 × 15 并发 → 0.1 秒
Flag：Geesec{19b7d146-27a7-4667-b8dc-8fbaea2f17fa}  🎉
总耗时：0.3 秒
```

**从"完全跑偏"到"精准命中"，0.3 秒。**

---

## 填坑：GUI 线程安全问题

升级过程中还踩了一个经典坑。并发引擎的 15 个工作线程全部回调 GUI 的 `_progress` 函数，而那个函数直接操作 tkinter 控件：

```python
# ❌ 旧代码：15 个线程同时写 tkinter → 主循环崩溃
def _progress(stage, item, status):
    self._solve_progress.insert(tk.END, f"[{stage}] ...")
    self._solve_progress.see(tk.END)
```

**tkinter 不是线程安全的。** 多线程同时操作，Tcl 解释器直接挂掉，界面一片空白。

修复方案：

```python
# ✅ 新代码：worker → queue → main thread → tkinter
import queue

def _progress(stage, item, status):
    self._gui_queue.put(('progress', stage, item, status))  # 只入队

def _process_queue():
    """主线程每 50ms 消费队列"""
    while True:
        msg = self._gui_queue.get_nowait()
        self._solve_progress.insert(tk.END, ...)  # 安全
```

简单但有效的生产者-消费者模式，15 个线程的进度更新全部序列化到主线程，再也没崩过。

---

## 完整改动清单

| 模块 | 改动量 | 关键变化 |
|------|--------|---------|
| `url_analyzer.py` | +400行 | SmartFingerprinter + ConcurrentEngine + AdaptiveScheduler |
| `gui.py` | +170行 | 线程安全队列 + 指纹显示 + 异常兜底 |
| Bug修复 | 6项 | POST降级、flag丢失、孤儿代码、路径硬编码、格式遗漏、线程崩溃 |

**零外部依赖**，全部基于 Python stdlib（`concurrent.futures`、`threading`、`queue`、`urllib`）。

---

## 使用方式

```bash
cd Yang-web
python -m yang_web.gui
```

1. 粘贴目标 URL
2. 点击 **分析** → 源码指纹 + 漏洞识别
3. 点击 **🚀 一键解题** → 并发攻击 → 拿 Flag

---

## 下一步计划

- **多步攻击链**：LFI → 日志污染 → RCE、SSRF → Redis → Webshell
- **WAF 绕过引擎**：自动检测 WAF 并选择绕过策略
- **请求历史记录**：方便复盘和 payload 调优
- **HTML 报告导出**：生成解题报告

---

## 结语

CTF 工具箱最怕的不是功能少，而是**指错方向**。

v2.0 的问题在于只看表象（URL参数名、路径特征），v3.0 学会了"读懂代码"。这个思路不止适用于 CTF——任何自动化安全分析工具，理解目标比盲目扫射重要一万倍。

---

*Yang-Web v3.0 | 开源 CTF 工具箱 | 零依赖 | Python 3.12+*

*项目地址：`C:\Users\阳\.qclaw\workspace\Yang-web`*
