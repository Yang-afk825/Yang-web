# Yang-Web v4.0 发布：从"命令行玩具"到"双击即用的桌面武器库"

> 上一版我在文末说：工具应该是"打开就用"。这一版，真的做到了——打包成了 exe，双击就开打。

---

## 起因：一个好工具，卡在了"环境"上

v3.6 的文章里，我吹了一整篇多阶段攻击链：LFI→日志投毒→RCE，一条链自动打穿。

但发完没多久，一个朋友来问我："你这个工具怎么用？"

我说：装 Python，git clone，pip 装依赖，python -m 启动……

他沉默了一下，回了一句："**我只是想双击打开它。**"

那一刻我意识到一个问题：**我的工具是给"已经装了 Python 的人"用的。** 可 CTF 赛场上，多少人是在别人的电脑上、比赛提供的虚拟机里、甚至只有 Windows 没有配置环境的机器上做题？

工具再强，打不开 = 0。

于是 v4.0 就一个字：**打包**。

---

## 核心变化一：双击即用的桌面应用

### 告别命令行，拥抱 exe

v4.0 用 **pywebview + WebView2** 把整个工具箱包进了独立的 Windows 窗口：

```
之前: 装 Python → 配环境 → python -m yang_web → 浏览器开 localhost
现在: 双击 Yang-Web.exe → 窗口弹出来 → 开打
```

打包后 **24MB**，零依赖，双击即用。UI 也从"浏览器标签页"升级成了**独立桌面窗口**，更像一个正经工具了。

### 打包路上的"坑"（值得单独说）

PyInstaller 打包 Web 应用，坑比想象的多，这里挑三个最典型的：

**坑 1：窗口模式启动即崩溃，还没日志**

exe 用 `console=False`（无黑窗口）启动时，`sys.stdout` 变成 `None`。uvicorn 的日志格式化器一调 `isatty()` 就炸——**服务静默失败，窗口也不弹**，连个报错都没有。

排查半天，解法一行：`uvicorn.run(..., log_config=None, access_log=False)`，绕开日志格式化。

**坑 2：JS 语法错误，整个界面"假死"**

界面点啥都没反应，F12 一看——**`await` 用在了非 async 箭头函数里**，整个 `<script>` 块解析失败，所有事件绑定全废。

教训：前端代码改完，先用 `node --check` 过一遍语法再打包。资源是固化进 exe 的，**改前端必须重打包**，改完不打包 = 白改。

**坑 3：脚本库 51 个脚本全"失踪"**

打包后点"运行脚本"，报 `expected str, bytes or os.PathLike object, not NoneType`。

原因：spec 的 `datas` 只打了 Web UI，**没打 scripts 目录**。exe 运行时 `SCRIPT_DIR` 指向 `_MEI` 临时解压目录里的空文件夹，脚本路径全是 None。

修复：spec datas 补上 `yang_web/scripts`，一行搞定。

---

## 核心变化二：SSE 实时攻击流

以前自动攻击是"黑盒"：点开始，转圈，出结果或不出。

v4.0 的自动攻击页改成了 **SSE 实时推送**——每一次攻击动作都实时流到界面：

```
[17:23:01] 阶段: 源码指纹 → 识别到 system($_POST['a'])
[17:23:02] 引擎: simple_cmd_rce → 提取参数 a (POST)
[17:23:03] 尝试: cat /flag → 命中!
[17:23:03] 🏁 FLAG: Geesec{...}
```

后端用线程池跑攻击链，进度经队列推给 SSE，前端逐条渲染。**看得到攻击在"想什么"**，对学习也有帮助。

---

## 核心变化三：脚本库靶场——"工具自证"

这一版做了件有点自恋的事：**给脚本库配了个专属靶场**，证明"脚本库里的脚本真的能解出题"。

靶场是单文件 Python（http.server + sqlite3，零依赖），起在 9999 端口，4 个关卡：

| 关卡 | 漏洞类型 | 脚本库对应脚本 |
|------|----------|----------------|
| L1 | 时间盲注（SLEEP 延时） | `time_sqli.py` |
| L2 | 布尔盲注（OK/NO） | `bool_inject.py` |
| L3 | JS 硬编码 Flag | `js_challenge_solver.py` |
| L4 | 命令执行（system()） | 自动攻击 simple_cmd_rce |

玩法：浏览器开 `http://127.0.0.1:9999/`，脚本库页填 URL + QUERY，一键解出 Flag。

### 做靶场的收获：比做题更深的踩坑

设计靶场的过程，等于把脚本库的脚本"反向测试"了一遍，挖出一堆平时发现不了的 bug：

**1. 时间盲注脚本：charset 里没有大写字母**

flag 首字符是 `G`（大写），而 `time_sqli.py` 的字符集是 `ascii_lowercase + digits + "_{}-"`——**大写字母根本不会去试**。首字符永远猜不到，整个盲注空转。

修：charset 加上大小写字母。

**2. 盲注脚本：URL 里有空格直接崩**

urllib 分支没对 URL 做编码，payload 里的空格触发 `InvalidURL: can't contain control characters`，异常被 `except` 吞掉，`elapsed≈0`——**永远"猜不中"**。

修：urllib 分支先 `quote` 编码 URL。

**3. 靶场正则：`SLEEP(2.0)` 匹配不上**

time_sqli 把延时 `str(2.0)` 生成 `SLEEP(2.0)`，靶场正则 `\d+` 只匹配整数——**延时没触发，真条件假条件都秒回**。

修：正则支持小数 `\d+(?:\.\d+)?`。

**4. 最隐蔽的：剥 HTML 标签把 PHP 代码块剥没了**

自动攻击引擎要从 `highlight_file()` 的源码里提取 `$_POST['cmd']` 参数。源码是带 `<span>` 高亮的 HTML，引擎先 `re.sub(r'<[^>]+>', '', source)` 剥标签。

结果：**`<?php ... ?>` 整个被当成"标签"剥掉了**——`<` 到 `>` 之间的所有代码消失，`$_POST` 无影无踪，参数提取永远失败。

修：剥标签前先把 `<?php ... ?>` 替换成占位符保护起来，剥完再还原。

---

## 为什么这些 bug 值得写

你可能觉得：一个工具给自己做靶场，修的全是"自己的 bug"，有啥好说的？

但反过来想——**这些 bug 全是真实 CTF 场景的缩影**：

- charset 缺大写 → 对应"flag 格式猜不到"的实战痛点
- URL 编码缺失 → 对应"脚本在 Windows 上跑不动"的环境差异
- SLEEP 正则 → 对应"靶场实现细节和预期不符"的坑
- 剥标签误伤 PHP 块 → 对应"正则处理 HTML 永远有边界情况"

工具不是写出来就完事，**得拿真实题目反复喂它**，bug 才会一个个浮出来。v4.0 的靶场，就是给脚本库做了一次"实战体检"。

---

## v4.0 全量更新清单

- 🖥️ **独立桌面应用** — pywebview 打包 exe（24MB），双击即用，无需 Python 环境
- 🌐 **内嵌浏览器** — 工具箱内置浏览器，支持 Headers 编辑 + CORS 服务端代理（绕过浏览器跨域限制）
- ⚡ **SSE 实时攻击流** — 自动攻击全程进度实时推送
- 🎮 **脚本库靶场** — 4 关 Web 挑战，脚本一键解出 Flag
- 🎯 **简单命令注入引擎（simple_cmd_rce）** — 针对 `system($_POST['x'])` 无 WAF 靶场，源码解析 + 参数提取 + 直连探测，秒出 Flag
- 📥 **脚本库 URL 输入** — 目标 URL + 额外参数输入框，整行参数传入，结果本页显示
- 🐛 **大量修复** — exe 窗口崩溃、JS 语法、脚本库表格空白、Python2 脚本迁移、GBK 编码崩溃、盲注 charset/URL 编码/正则

---

## 下载 & 使用

```bash
git clone https://github.com/Yang-afk825/Yang-web.git
cd Yang-web
python -m yang_web.server    # 源码跑 Web 版

# 或直接用打包好的桌面应用
dist/Yang-Web.exe            # 双击即用
```

靶场玩法：

```bash
python 靶场.py 9999
# 浏览器打开 http://127.0.0.1:9999/
# 脚本库页填 URL + QUERY，一键解 Flag
```

---

## 文末

从 v1.4 的命令行脚本，到 v2.0 的 GUI，到 v3.0 的密码引擎，到 v3.6 的攻击链，再到 v4.0 的桌面应用——Yang-Web 终于长成了"**双击即用**"的样子。

下一版想做什么？可能是更聪明的靶场自动生成，也可能是把攻击链可视化得更好看。

老规矩：**好用就点个 Star**，有想法就来 GitHub 提 Issue。工具免费开源，但你们的使用和反馈，是它活下去的燃料。

---

*Yang-Web · MIT License · [github.com/Yang-afk825/Yang-web](https://github.com/Yang-afk825/Yang-web)*
